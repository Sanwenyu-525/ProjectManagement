# 代码质量修复规划

**基于**: docs/CODE_REVIEW_2026-06-24.md
**创建日期**: 2026-06-24
**总问题数**: 90（High 12, Medium 40, Low 38）

---

## 分阶段策略

| 阶段 | 目标 | 问题数 | 风险 |
|------|------|--------|------|
| Phase 1 | 死代码清除 + 低风险一致性修复 | ~25 | 极低 |
| Phase 2 | 类型安全 + 错误处理 | ~20 | 低 |
| Phase 3 | 性能优化 | ~12 | 中 |
| Phase 4 | 架构重构（大组件拆分） | ~8 | 高 |

---

## Phase 1：死代码清除 + 一致性修复（本轮执行）

**原则**: 每个改动独立可验证，不改变运行时行为，不引入新 API。

### 1A. 死代码清除

| # | 问题 | 文件 | 改动 |
|---|------|------|------|
| 1A-1 | workspaceStore 未使用字段 | `stores/workspaceStore.ts` | 删除 `splitPaneOpen`, `splitRatio`, `splitVerticalOpen`, `splitVerticalRatio`, `leftPane/rightPane/topPane/bottomPane`, `tabBarWidth` 及其 setter |
| 1A-2 | agentStore 废弃字段 | `stores/agentStore.ts` | 删除 `toolEvents`, `appendToolEvent`, `clearToolEvents`, `removeMessagesFrom` |
| 1A-3 | AgentTabBar showClose 常量 | `AgentTabBar.tsx` | 移除 `showClose = true` 和条件判断 |
| 1A-4 | GitTab 恒等三元 | `GitTab.tsx` | 移除 4 处 `isDark ? X : X` 相同分支 |
| 1A-5 | ClaudeProvider 未使用构造参数 | `ClaudeProvider.ts` | 移除 `_config` 参数 |
| 1A-6 | OverviewTab activityTab | `OverviewTab.tsx` | 移除未使用的 activityTab 状态或实现过滤 |
| 1A-7 | NewProjectWizard 占位步骤 | `NewProjectWizard.tsx` | 简化为单步骤，移除 workspace/agent 占位 |

### 1B. 一致性修复

| # | 问题 | 文件 | 改动 |
|---|------|------|------|
| 1B-1 | sessions.rs UUID 不一致 | `sessions.rs` | `uuid::Uuid::new_v4()` → `crate::db::new_id()` |
| 1B-2 | Import type 修饰符 | `WorkspacePage.tsx` | `TerminalExitEvent`, `TerminalOutputEvent` 改为 `import type` |
| 1B-3 | 底部面板硬编码色 | `BottomPanel.tsx:345` | `#ffffff` → CSS 变量 |
| 1B-4 | MainLayout 硬编码色 | `MainLayout.tsx:583` | `#fff` → CSS 变量 |
| 1B-5 | NewProjectWizard 原始 HTML | `NewProjectWizard.tsx` | `<input>` → Ant Input, `<button>` → Ant Button |
| 1B-6 | GitTab isDark 死三元清理后简化 panelBg | `GitTab.tsx` | 移除硬编码 `#ffffff` fallback |
| 1B-7 | AgentTabBar localStorage 重复 | `AgentTabBar.tsx` | 提取 `lastCwd` 计算值，消除 5 次重复调用 |

### 1C. graphClassifier 缓存键 Bug 修复

| # | 问题 | 文件 | 改动 |
|---|------|------|------|
| 1C-1 | 缓存键碰撞 | `graphClassifier.ts` | 将节点路径排序后拼接 hash 纳入缓存键 |

---

## Phase 2：类型安全 + 错误处理（后续）

- W-M4: WorkspacePage gitLog 不安全断言
- W-M6: AgentRightPanel toggle 逻辑 bug
- W-M10: CodeEditorPane eslint-disable 修复
- W-M11: WorkspacePage listen() 错误处理
- P-M1~M3: ProjectsPage 类型安全
- P-M5: ProjectDetailPage 类型断言
- P-M7: useProjects `id!` 断言
- S-M7: themeStore localStorage 验证
- S-M11: graphClassifier JSON.parse 验证
- S-M13~M14: FileExplorer 错误处理
- R-M1~M2: SQL 列名插值安全
- R-M5: batch_import 输入验证

## Phase 3：性能优化（后续）

- W-M3: WorkspacePage handleTabClose 闭包优化
- W-M8: AgentTabBar selector 优化
- P-H3: ProjectsPage branch fetching 优化
- S-M3: SearchBox commands 缓存
- S-M4~M6: FileExplorer 性能优化
- R-M3: graph.rs 正则 LazyLock
- R-M4: graph.rs 事务包装

## Phase 4：架构重构（后续）

- P-H1: ProjectsPage 拆分（1285→多组件）
- P-H4: GitTab 拆分（607→多组件）
- P-H5: GraphTab 拆分（925→多组件）
- W-H2: WorkspacePage 拆分（593→hooks+组件）
- W-H3: ClaudeProvider 拆分
- S-H1: SearchBox 拆分
- S-H2: FileExplorer 拆分

---

## 执行标准

每个修复完成后运行验证：

```bash
npx tsc -b          # TypeScript 编译
npx eslint src/     # Lint 检查
```

零错误才算通过。
